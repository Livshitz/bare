import pug from 'pug';
// import compilePug from 'html2jade';

// import pug from 'pug';
// const compilePug = (template: string, options: pug.Options) => {
//   return pug.compile(template, options);
// };

export { pug };
